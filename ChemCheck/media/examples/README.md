# Example broken mechanism files

This folder contains example kinetic models that have errors in.
Currently, published models, with syntax errors in (as opposed
to just high rate coefficients etc.)


